## WFP 通信协议与原系统线索整理

### 1. 关键结论（可直接对接硬件）
- **通信方式**：UDP（非串口）
- **控制指令格式**：`5A A5 [编号高位] [编号低位] [颜色] [效果] 00 60`
- **控制指令发送地址**：`192.168.0.169:20109`
- **数据上报监听端口**：`8080 / 8899`
- **广播端口**：`7788`
- **编号字节序**：低字节在前（Little Endian）

---

### 2. 原系统反向工程线索
来源文件：
- `qifupai2/Assets/getzhiling.cs`
- `qifupai2/Assets/UDPSender.cs`
- `UDP通信与硬件配合需求文档.md`

#### 2.1 “thetest1” 相关逻辑
在 `getzhiling.cs` 中：
- `thetest1 = int.Parse(thetest)`（原始编号十进制）
- `theget16()` 将十进制编号转为**两字节十六进制**
- 输出格式：`"高位 低位"`（带空格）
- 后续在组装指令时**交换顺序** → 低位在前

#### 2.2 编号转换规律（字节序）
逻辑结论：
- 编号转换为两字节后，**发送顺序为低字节 + 高字节**

示例：
- 编号 `00001` → 转十六进制 `00 01` → 发送 `01 00`
- 编号 `12345` → 十六进制 `30 39` → 发送 `39 30`

---

### 3. 控制指令格式（最终落地）
标准指令：
```
5A A5 [设备编号低字节] [设备编号高字节] [颜色] [效果] 00 60
```

字段说明：
- `5A A5`：指令头
- `[编号低字节] [编号高字节]`：设备编号（Little Endian）
- `[颜色]`：颜色码（见颜色枚举）
- `[效果]`：效果码（常亮/呼吸/跳动）
- `00 60`：指令尾

---

### 4. 原系统指令拼装细节（重要）
`getzhiling.cs` 中组装逻辑（简化）：
```
zuizhongzhiling =
  the1[0] + " " + the1[1] + " " +
  thett[1] + " " + thett[0] + " " +
  the1[3] + " " + the1[4] + " " +
  the1[5] + " " + the1[6] + " " +
  the1[8] + " " + the1[7];
```
说明：
- `the1[2]` 是设备编号（十进制），转十六进制后再交换顺序
- 末尾两个字节有**交换顺序**（index 8 和 7 互换）
- 说明原始后端返回字段顺序可能不符合硬件要求，需要重排

---

### 5. 原系统通信流程（验证可用）
1. Unity 客户端定时轮询后端（`thepc_getkongzhi`）
2. 获取控制指令（字符串格式）
3. 转换设备编号十进制 → 十六进制
4. 组装指令（低字节在前）
5. UDP 发送至设备 `192.168.0.169:20109`
6. 硬件通过 UDP 上报电量 / 编号（8080 或 8899）

---

### 6. 必须向硬件厂商确认的事项（清单）
- 指令头 `5A A5` 与指令尾 `00 60` 的含义
- 编号字段的字节序（确认 Little Endian）
- 颜色字段取值范围与含义
- 效果字段取值范围与含义
- 是否存在校验位（原系统未发现）
- 硬件上报数据格式（字段位置 / 字节序）
- 端口是否可配置（20109 / 8080 / 8899 / 7788）

---

### 7. WFP 对接建议（直接可用）
- 使用 UDP Socket 发送控制指令
- 设备编号转十六进制后交换字节顺序
- 发送格式严格按：`5A A5 [低位] [高位] [颜色] [效果] 00 60`
- 接收端监听 8080 / 8899，解析编号与电量

---

### 8. 关联文件路径
- 设备通信协议文档：`祈福牌源码/UDP通信与硬件配合需求文档.md`
- 指令转换逻辑：`祈福牌源码/qifupai2/qifupai2/Assets/getzhiling.cs`
- UDP发送逻辑：`祈福牌源码/qifupai2/qifupai2/Assets/UDPSender.cs`

---

### 9. 备注
本文档基于原系统反向工程整理，最终协议以硬件厂商确认文档为准。
